#ifndef HASH_SHA_256_SHA256_H
#define HASH_SHA_256_SHA256_H

#include <stdlib.h>
#include <string.h>

char* hash(char* msg);
char *strcatarray(char * dest, char *strings[], size_t number);

#endif //HASH_SHA_256_SHA256_H
